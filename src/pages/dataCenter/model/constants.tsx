export type ColumnType = {
  modelId?: string;
  modelName: string;
  proportion: number;
};
//测试数据
export const dataTest = [
  {
    typeId: '222',
    name: 'string',
  },
];
export type SearchModel = {
  modelId?: string;
  modelName: string;
  proportion: number;
};

//将字符串转换成字符串数组
